# Sentence Laundry (문장세탁기) 🧺

[![English README](https://img.shields.io/badge/README-English-red.svg)](README.md)

Sentence Laundry는 텍스트를 여러 언어로 순회시켜 "세탁"해버리는 자동 번역 도구입니다. 언어 간의 어순, 조사의 차이, 미묘한 뉘앙스 차이를 이용하여 문장을 전혀 다른 의미로 재탄생시킵니다.

## 주요 기능

- **표준 세탁 (Wash)**: 정해진 순서대로 번역을 수행합니다 (예: 한국어 -> 영어 -> 일본어 -> 한국어).
- **랜덤 탈수 (Random Spin)**: 독일어, 아랍어, 베트남어 등 랜덤한 언어를 중간에 섞어 결과를 예측할 수 없게 만듭니다.
- **세탁 지수 (Laundry Score)**: 문장이 얼마나 "깨끗하게" 세탁되었는지(망가졌는지) 수치로 보여줍니다.
- **가상 환경 지원**: `venv`를 이용하여 독립된 환경에서 실행됩니다.

## 설치 방법

```bash
# 가상 환경 생성
python3 -m venv venv

# 가상 환경 활성화
source venv/bin/activate

# 의존성 설치
pip install -r requirements.txt
```

## 사용 방법

### CLI로 실행
핵심 로직을 직접 테스트해볼 수 있습니다:
```bash
# 가상 환경 활성화 후
python3 translator.py
```

### API 서버 실행
FastAPI 서버를 시작합니다:
```bash
# 가상 환경 활성화 후
python3 main.py
```
서버는 `http://0.0.0.0:8001`에서 실행됩니다.

## API 엔드포인트

### 1. `POST /wash` : 표준 세탁
정해진 언어 체인을 따라 문장을 순회 번역합니다.

*   **Request Body**:
    ```json
    {
      "text": "세탁할 문장 (필수)",
      "chain": ["en", "ja"] (선택, 기본값: 영어->일본어->한국어)
    }
    ```

### 2. `POST /random-spin` : 랜덤 탈수
중간 단계의 언어를 무작위로 선택하여 문장을 더 강력하게 왜곡합니다.

*   **Request Body**:
    ```json
    {
      "text": "세탁할 문장 (필수)",
      "random_count": 3 (선택, 거쳐갈 랜덤 언어 개수, 기본값: 2)
    }
    ```

---

### 응답 구조 (공통)
모든 요청은 다음과 같은 상세한 "세탁 리포트"를 반환합니다.

```json
{
  "original": "원본 문장",
  "laundered_result": "최종 세탁된 문장",
  "chain": ["ko", "en", "ja", "ko"],
  "steps": [
    { "src": "ko", "dest": "en", "text": "..." },
    ...
  ],
  "metrics": {
    "length_diff": 4,
    "similarity": 0.71,
    "laundry_score": 28.57
  }
}
```

---
"왜 세탁기인가요? 문장의 얼룩(원래 의미)을 쏙 빼드리기 때문입니다!"
